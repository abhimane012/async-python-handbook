[project]
name = "asyncio-real-world-examples"
version = "0.1.0"
description = "asyncio-real-world-examples demonstrates real-world async Python patterns using asyncio."
requires-python = ">=3.14"
dependencies = [
    "aiofiles>=25.1.0",
    "aiohttp>=3.13.5",
    "rich>=15.0.0",
]
